import 'package:flutter/material.dart';

class Ragpage extends StatelessWidget {
  const Ragpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Rag Model"),
      ),
      body: const Center(
        child: Column(
          children: [
            const Text("Impossible to Implemented in 48 hours")
          ],
        ),
      ),
    );
  }
}
